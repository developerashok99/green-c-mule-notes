Good morning once again. Today's agenda is property sectionalization. We have fewer questions in interviews, but we are utilizing this compulsory in real-time work. This concept is very important. because the work is done in real time. This is the acceleration of properties and creating property files for different environments. And one more thing is deploy the application using run configuration. Till now, I have been going to the application, right-clicking here and I am doing it here. There are other options as well. We will see how to do and what to do in today's session. Along with that, basically securing properties is also a concept. Let us also see how to secure those properties. First of all, what exactly are these properties? Why are we externalizing them? We need to understand that first. Now, what have we done? We have developed this application. Okay, we have developed this application. Here, for example, if you observe, we have discussed different environments in real time. Do you remember? What are the environments? and you. I will take this code and test it in UAD. It is very good. There is nothing wrong with the dev key. Use only one for dev key, SID and UAD. That is fine. We don't have many environments. We have only one environment. You said you will use only this for UAD. We have used this for UAD. If I take the same application and deploy it in production, will their production details go? Will it not go? It will not go. It will not go, right? I will tell you why it will not go. the server. They go to UIT and get their UIT details from our application production. Do you have original details in UIT? Do you have actual live details or test details? Test details. At that time, it was wrong mapping, right? Is it correct or wrong? I don't understand. I don't understand. It is correct. It was wrong mapping, right? It was wrong mapping. What should I do now? Will it be correct if I externalize these details or not? I will externalize these details and create a property file for each environment. I will fill up the details related to which environment and map them dynamically. That's what externalization of properties is. This property is called host. This property is called port. This property is called path. This property is called secret key. I will try to externalize these properties. externalize to a property file and for each environment I will create a separate property file and I will give the respective details. Do you have any doubt about this? No, I have just touched it. Ideally, we should have only three environments. Dev, SID, UAT, Pre-Prod, Prod, DR. But in this particular application, we have only three environments. Dev, UAT, Prod. Ideally, we should have these three environments. But we don't need them that much. So we have these three environments. Okay, fine. This is used for integration testing and UAT. Is stage enough? What is stage? Before production, anything is stage basically. Yes sir. If one is done with performance, the next step is directly going to stage LT. Yes sir. In stage, we can do tester next to UAT. In LT, load testing is pre-prod. Is it enough? Yes sir. Prod? Prod is there. That's what we thought, but the names are different. That's it. That's what we thought, but the names are different. That's it. That's what we thought, but the names are different. That's it. That's what we thought, but the names are different. That's what we thought, but the names are different. That's what we thought, but the names are different. That's what we thought, but the names are different. That's what we thought, but the names are different. That's what we thought, but the names are different. That's what we thought, but the names are different. That's what we thought, but the names are different. That's what we thought, but the names are different. That's what we thought, but the names are different. That's what we thought, but the names are different. Or some people call it post prod. Post prod is something that happens after prod. Some people do the same thing in prod as well. See, different terminologies, but ideally it would be like this. Okay, now I am developing this particular application. Where should it be deployed? It should be deployed in dev and uat prod. I am using two connectors in that. One is third party and the other is rest service. For example, I am using our database here. The database also has 3 environment related details. The rest also has 3 environment related details. For example, take this weather API. This weather API is the host for dev environment. dev.api.openweather.org This is the host. For UAT, UAT.api.openweathermap.org For production, API.openweathermap.org This is the host. and the secret key is also changing for each of them. This is the rest service. For the database, dev chemo is 10.1.25.50.330. Then for the UAD, host is 10.1.25.51 and 10.1.25.52 for production. Now, if I go to my particular configuration and hard code these details, No matter where I deploy it, it will only fit the hard-coded details. That's wrong, right? That's why I have to create a property file. For example, I will create a property file for dev, a property file for uat and a property file for production. In the dev property file, I will mention the request details related to dev and the database details related to dev. In uat, I will mention the details related to this, uat details and uat details related to this. What will happen then? I have mentioned it, it is good. But when I deploy in dev, I have to refer dev property file. When I deploy in UAT, I have to refer UAT property file. When I deploy in production, I have to refer production property file. I will set a configuration. Then I will deploy the same application in dev, it will pick dev. If I do it in production, it will pick production. What will happen then? As it is, the mapping is as it is. When I deploy it in production, it will take the production property file and pick these values. Does this application work properly or not? It works. Overall, we have to do this for every application. We can do it in two ways. properties can be done in two types. .aml and .properties. Each combination is done in one way. That is a simple syntax change. Both are the same, but there is a syntax change. There is a kind of syntax in this. Syntax will be there in this. I will show you two. An organization will use an ML and an organization will use properties. There is no significance in that. Basically, if you use this, there will be more performance. There is no place where you can use this. You can use anything. But it depends on the architect. Like, let's use this. Let's use ML. Let's use properties. Each person has a different significance. Each person decides in their own way. There is no technical performance related issues or something like that. That's why... We can use anything. So we will do the syntax in .ml once. Then we will do the syntax in .properties. These are the same steps for both. Now what are the steps? This is the first step. Prepare property file for each environment. For example, if I have 6 environments in my organization, then how many property files should I do? You have to do 6 hands. What if there are 2? You have to do 2. What if there are 4? You have to do 4. Prepare property file for each environment based on the number of environments, exactly. Okay, clear. Then, Configure, Configuration property global element. Next, configure the properties in components. Only the host will change, generally. The port path is ok. This host will change, actually. Now, should I dynamically do this host in this HTTP configuration or should I not do it? I will do it. What should I do then? What do we do in this particular property file? We have assigned a value to the host, right? This is the host. This is the value. Here we have to refer to this. How do we refer to it? We refer to it like this. Dollar, curly braces, this host. This is the key name, right? This is the key name. This is syntax. Standard. We have to follow it like this. Where? Now I go to the host, which is the configure for the HTTP request. I go to the host. How do I refer to it? I have to refer to it like this. What happens then? Ok, because of this particular configuration property, we have to go to UAD and pick the UAD property file. Ok, that's good. After that, we have to go to this request configuration and pick the host. We have to pick this particular host in the UAD property, in the property file. This is what it tells us. Is it clear for everyone? Is there confusion? Please tell me once. For example, this is the property file. Dev, uat, prod. Which property file should I pick? I am deploying the application in dev. Which property file should I pick? I should pick this one. Got it? Actually, according to us. What defines that? This one. This configuration property. Okay? Now, in our dev, in our application, I am calling this weather API. Got it? Weather API. Here, in this weather API, the host, in dev cover, you. When I mention the host in curly braces, it will go to the property file and search for the host. It will take the value of the host and mention it here. Sir, do we give the same host even in the test product? Why do we give the same host? It will change, right? Like the keyword host. The keyword host will be the same, but the value will change, right? The value of the host will change, right? Now the host. For example, dev.openweatherap.org Now, this is the dev key. UAT is the host key. But here, UAT dot... This value changes, right? Now, let's give the host in the requestor. Okay. Now, it is clear? Yes, it is clear. Okay, fine. Next. Next. So, now we will do this in the configurations. Another thing is, when we write data view expressions, this particular syntax won't work. Instead of this syntax, leave this too. P should be like this. Normal braces should be placed like this. In single quotes. This is syntax. I don't have any rule as to why we should do this again. When you want to use the data view expression, when you want to get it from the property file, you can give it a small p and single quotes as a key name. For example, since the key name we gave is host, it can be any other host or any other key. What should we do next? We can pass it in runtime arguments. Run application will have a feature in it, you have to deploy it. If you right click it, it will not work. There will be a small setting, you have to do according to that setting. Okay? Fine. These are four steps. We will implement the same steps now. For any application, for the application we developed in the previous session, we will be trying to do that exactly. Is it clear to everyone? Let's go to the demonstration. Any more theoretical doubts here? No. Okay. Now, for example, this is our application. Oh sorry, it shouldn't be here. This is our transformation. Now I have to create property files like this for this application. Where do we create property files? I told you about the project structure, right? When I tell you about the project structure, what did I say? In the main mule of SRC, main important components and project related main resources, project files are created in the main mule of SRC. Now, where do we create the resources to run this project? In the main resources. Now, isn't this project a necessary resource? Isn't it a property file? To run it? That's why we keep it here. What I do is, generally, we can create a folder here and organize it in that folder. Otherwise, some people will directly create it here. But the best way is to create a folder here and in that folder, It is better to create all the property files in all the environments. Now the best way is to create a folder. I will create the first folder. New folder. Here you can see the option folder. I right-clicked there, right-clicked and clicked on the new folder. I am giving the folder name. Most of the time, keep the folder name as config. The configuration properties will be clear. You can give it as you wish. That's fine. No issues. But when we go to the project, Okay. Depending on the name, we will also give the file name. What is our first environment? dev.yaml What is our file? yaml What is yaml file? It is a simple syntax. It is also a type of file. Text file and yaml file are the same. The syntax is slightly different. I will show you how the syntax is in the yaml file. Now we have to create three files. I created one file. Click on finish. I gave the name. As soon as I gave the name, the file was created. This file is created and opened here. Now I have to create many files like this. I have to create two more files. Now go here and right click and select the new file. Or else click on this and right click. Then right click on that and copy and paste. Or else you can copy and paste. Or else click on this and control C and control V. Control C for copy and control V for paste. Then another file will be created. You can do it like that or like this. It depends on our convenience. The other file is getting processed. You can do two types of files in one config. Amlu and properties. Can I do both? You can do it. But it's not required, right? Honestly. Now this is dev and uat, right? I changed the name here. uat and then next new file. I am creating a new one this time. I have created three files. After creating three files, I have told you to create everything related to this. What should we externalize in general? Not only this, everything related to the listener should be externalized. To put it simply, I will explain it here. This is the aml file configuration. In the aml file, the format is like this. First, I will explain everything related to the listener. What did I do for that? I put a heading. Why did I put a hash in the heading? Hash means in aml it is a comment. That is, it will treat this particular file, this particular line as a comment. That is why I have put this first hash, right? I have put hash and heading like this. Why am I separating them one by one? To put it like this, how many we are configuring, we are configuring with two details here. But in real time, we have to do 10-15 configurations. How do we understand which one is where? What if we put the headings? This is listener configuration, this is request configuration. If there are five request configurations, this is weather request configuration, this is second request configuration, this is third request configuration. Since we have clarity, we can put headings like this. This is the best practice. If many people do it, they will make it worse. Don't do it like that. Whichever configuration is good, we will mention the name like this. After mentioning the name, the details related to it. Request whether Exactly. Okay. It will change, no matter where you deploy it, it will remain the same. This is local, right? Even if you take it to production, it will automatically change to host when you upload it to cloud. Okay, okay. It will not pick it. But there is only one difference. Why do you do it? Generally, you cannot host it. You have to port it. Port is compulsory. Why is it necessary? When deployed in the cloud hub, if our HTTP service is 8081, and if HTTPS port is 8082, and there is a shared load balancer concept called dedicated load balancer, then it has to be 8091 and 8092. That is why the listener configuration has to be fully actualized. But I will tell you when that situation comes. In real-time, compulsory listener configuration and all other connector configurations are also externalized and defined in a property file. Now, I am basically saying that this is a listener. What I will do for that is, I will say that the naming convention should also be the same and put it like this. Now, I have put a colon for this HTTP. If I click enter after putting a colon, if I give a tab, now for each of them, HTTP.Listener, host again in the listener. This is my configuration in general. Then there is a listener inside the HTTP. Inside the listener, there are these details. Exactly. Host, port, path. That is, this colon, this space, this tab space, actually also. What is this? HTTP for HTTP. There is a listener inside and these three details are inside the listener. This is the clarity. Now I have given the HTTP colon first, then enter, tab. Then I have given the listener in this tab, and these three details are below the listener. This is a syntax. The reason for writing it like this is because it is like an ML syntax. The HTTP colon means that since I have given space below the colon, I have given a tab space, so the listener is inside the listener. Now you might have a doubt. How do we define the host? How do we define the port? So why do we have to write it 3 times? For example, we can define this as well. If not now, what will be my problem? Everytime, there is a port, I have to write this again. Without writing it like that, I wrote it in the best way. This is the port, this is 8085, this is the path. You can write it like this, I wrote it like this. Is there any doubt for everyone who says it is clear? Yes, it is clear. It is related to weather. Here we have a heading, here we have a heading, here we have a heading and again it is related to weather. In an API, there may be an option to call multiple request services, right? Therefore, it is better to call any request if the naming convention is correct. This is the host, port, path and application ID that is related to weather. How is it related to dev? Actually, who will give these details now? Third party. Third party, weather API people will give. What will you do then? You will mention the same in DEV and in UAT. But we have 3 property files, we have to mention them in 3 of them. But if they have 3 DEV and UAT products, they will mention DEV, UAT and PROD. So don't get confused. No one is going to tell me all the points that I have said. I am trying to discuss more here. I have faced some things. I have understood that I need some time to understand. That is why I am telling you all this. There is a prod. Even our UAT has to use their DEV. There are such situations too. But still, what have I done here? I have separated DEV from DEV, UAT from UAT, and PRODE from PRODE. But it didn't work. I just configured it to show the difference. Because they are using 3API, they didn't give it to separate environments. Exactly. But in real time, we connect with this particular 3rd party REST API. What are your configurations for DEV? What are your configurations for UAT? What are your configurations for that product if you have three environments? If not, you have two environments. Should we use this for UAT as well? Should we use this for DEV as well for UAT? After confirming these things with them in the mail communication, then you have to configure respective details. Okay, fine. Now, the host related to the weather is sufficient. weather.host, weather.port, weather.path, weather.port, weather.path, weather.port, weather.path, Do I have to give HDP for this? You can give it. Here also I don't have to give anything. There is no need to give it directly. It will be enough if you give HDP and keep only the listener. Actually now I have to remove this and show it as a convention. There is a single step here and there is a double step here. I just wanted to show it. Here also we have Http and weather. Otherwise, we have Http here. We can start from weather here. Like this also. But still, since this is separate, I don't want to again mess it up with this one. Listen to my configuration. But I wanted it separately, so I commented on this and added a heading here. Many people don't add headings, so we should be careful. copy copy copy copy copy copy But since this will be useful when we write syntax, we will save it in the notepad. Okay, right. And again, we have to perform htp.listener. So, I copied and pasted it here. Now, this is okay. It's good. For production? For production, copy the same details and develop it. The same details, but the values will change. That's it. The values will change. Now, if you see. In real time, we will fill up the relevant details randomly. We did it randomly here also, but still you have to do it according to the details provided by that particular third party APA. See how these details change. This port won't change, it will be the same for both DEV and UAT. It won't be the same for both DEV and UAT. It will be the same for both DEV and UAT. It will be the same for both DEV and UAT. You can see how it works. You can see how it works. You can see how it works. You can see how it works. You can see how it works. You can see how it works. You can see how it works. Many people don't know what to do, they just do it one by one. When you have to do a lot of details at once, this is very very useful. What did I do? I selected all of them. Then I did CTRL forward slash. What about uncomment? CTRL forward slash. Again, same thing. Selecting the same thing again, commenting is enough. Now, for example, these three comments are done. I am selecting three. CTRL forward slash. Done. I have to comment again. CTRL forward slash. Shortcut. Control, shift, question mark. Not control, shift. Only control, forward slash. That symbol has a question mark and a forward slash key, right? Click on that. Control, shift. It's shifting. Only control. Control and then forward slash. Okay, fine. Now this is a shortcut for us. Actually too. Okay, now this should be in the comments. Let's save this too. Now all the details have come. We have done the first step. Prepare property file for each environment. Is it done? Yes. What is the next step? Configuration. Property file in global elements. We should do it in global elements. Where is this global element for us? In the flow element. In the flow element. In this global element, we don't need this Salesforce. We are just deleting it for this. We have to create a global element. How do we create this global element? Look here. Create. What element is it? Configuration property element. Where is it? Look here. There are four categories. In these four categories, in global configuration, there are configuration properties. Even if we don't do this, we can search here. Even if we search for configuration property, it will come to us. For example, if we do this, it will come to us. Even if we don't search, in global configuration, We will expand it and select the configuration properties. Now we need to set which file to select. Which file should we select? We need to do it dynamically, right? Exactly. That's why we will click on 3 dots and see the dev.aml. I have selected this. For example, I have done a dynamic hard-coded file. As you can see here, in config, what does it do? Does it take the entire path or does it directly take the config? It takes the config. SRC should also take the main resources, right? Why didn't it take it? Because it knows. It will check the resources first. In resources, in the config folder, it will decide this particular file. But what I am doing now is, am I doing the hard-code or am I getting picked to that environment? It will not stop, right? I did a hard code. I did a dynamic here as well. Now, it will backslash automatically. Actually, this is not a problem. If it works, if you ever have a problem, try to convert it to forward slash. That is fine, exactly. No problem. Even backslash also will work. When you have a problem, just observe. If it comes locally, it will not give much. Then convert forward slash. Now, Where do you pass the environment name? Inside the file? No, in which environment we are deploying the application, we are supplying the environment in the configuration related to that environment. For example, when I deploy UAT, UAT will give the environment name. I will apply it in prod and give the prod name. What is the parameter name for that? There will be a name and it will be decided by the organization. For example, let's say the name is mule.env. This is decided by the architect and this is the environment level property. It can be mule.env or env or mule.env. What I have done is mule.env. Yes. Yes. Okay. This is a clear configuration. This is very important. Okay? Now I have done it. This is safe. Is the second step done for us? This is done. First step is done. Second step is done. Now I will go to the listener. If I go to the listener and check, now in the listener configuration, what did I say? I am hard-coding all of this. It is not possible if I am hard-coding all of this, right? Where should we take the details from? From the property file. From the property file. What is the syntax for that? This is it, right? I am lazy because typing is my passion, that's why I copy and paste. I use Ctrl C and Ctrl V a lot, actually. Okay? I will go here now. Go to listener configuration and edit. What should we do here? Select this, delete this and dollar. What are we doing first? I'm going to. Control C. Control V. We will change the host, port and path of the listener. Now we will change the request configuration. We will click here to change the request configuration. We will click here to change the host. We will click here to change the host. Okay. Okay. Okay. How do you observe this? This comes under the data view expression. If you click the fx here, see how this is. This is the data view expression. If you check the xml file in the background, it looks like this. How do I give this? Do I have to give a dollar or a P? You have to give a P function. Okay. Okay. in the data. dynamic. Okay. Now, how do we deploy this? See here, is there a run? Go to run, run configurations or debug configurations. If you want to run, go to run. If you want to debug, go to debug. Whether you do it in run or debug, both are available. Exactly. Now, for example, go to run configurations. Here, we give the details related to the API. We pass the arguments related to it there. or environment variables or arguments or whatever we pass and basically if we send the details related to that variable, we take that and deploy it. So how to do it? Now run, run configurations. What are you saying? We are saying run but with some configurations. Is that clear? Yes. Simple. That's all. Now what is our application name? Here, our application name has come. Here, see already. How to create an application if it doesn't come? Should I create an application or should I create something else? It is up to you. I am creating a new configuration here. The name of this new configuration is prod-configuration. This prod-configuration is already there. I have set it to 1. What are the applications that come here? I can see all the applications that I have opened here. Is it? But which one should I deploy now? Our 7303 application. That's why I selected this one. Then this particular configuration will apply to any condition. Only this one will apply. As if it has been ignored. Okay fine. Then I will go to the environment here. Which variable should I pass? It should be the same name. What this will do is, the whole organization will set these parameters, mule.env or onlyenv. The architect would have already decided that name. Since I have given mule.env here, that is mule.env. Now what actually works for me is not dev, but prod. I have given you two dummy configurations, right? If you get prod, it will work for me, so I will give prod. It will ask you to save it. Save it. Now, we will close all the projects. Close unrelated projects. Now, all of these will be closed here. All of these will be closed and only the files related to these projects will be closed. Ok, it is open. Ok, there are raml files. Close taps to the right. I just wanted to. Since we have given the production video details and configured it properly, it will work with the details. What we will do is, we will go and hit it once. Why is this not working? If you remember, did you see what we did with the listener configuration? We have given it as weather and in the past it was weather slash city. Now it will not work if we give it as weather slash city. Did you understand it clearly? Yes. Why is it not working now? Yeah. Yeah. with these arguments null and number you call the function null with minus Now after it is deployed, it will work. Now I will hit it. Another error is coming. What is the error? You call the function hyphen hyphen. Now let's see what happened. Let's see if I have written anything wrong. WEATHER response is correct. This is also correct. Let's find it. Let's go to the console and try to see. Another null is coming. Let's check why null is coming. Now what should I do now? I will do it in debug. You can use the same configuration for this, if you have already checked. Everything is ready. Even if you do it in run, there are options for debug. Come here and debug. After setting this, if you right click here, the same setting will be applicable. After you click here, it is applicable. I have already added a breakpoint here. If you hit here, it will stop. Where should we go? We have to come here. Okay, let's hit. Okay, let's debug. Next, next, next. The response has come. Look at the variable now. City not found. That's the problem. That means we are sending the city wrong. We are sending the city wrong in the body. That's the problem. What can I say? I checked the weather response. If we check the weather response, 404. What did we do? 404. We added the success status code value. Do you remember? Otherwise, it would have been error here itself. 404. Okay. you. No, no, no. UAT file and 80 and path. Okay. Dev.properties and prod.properties will have a different extension and there will be a difference in the syntax inside. That's it. Ok, fine. You have understood that too. Ok, sir. It will be done. Now, let's go here. Sir, in this, my dot is small dot. In this, we are saying that we could not resolve the address, right? What should we change with this? Actually, we don't have their UAT, right? Basically, we have to tell them. We have an issue because we don't have the right value. I put it randomly because I want UAD here. If they give it correctly, it will work. But the host has to be changed. Yes, the host has to be changed. If they give a different quote, they have to put a different one. But we have to validate everything correctly. Now, let's see the syntax of the dev.properties file. How does the syntax look like? Is there any mark or is it there? Okay. There is a config1 on the right. I will copy it. Copy. I will paste it in our project. Where will I paste it? In main resources. Why did I put config1? Because there is already a config. Because there is already a config. For the difference, I will show both in the same one. Not separately in the other one. Now I will do this. Quick question, can we add these properties and files in that config? Yes, yes. Now, it will refer to this, but it is not referring to this, right? Now, it should refer to this. Where should I go? I should go to global, right? Yes. Where are we going? Where did we go? This will pick to .iml, but now, if I pick .properties, it should refer to config1. Right. the properties. Debug, wow, product config 1. The value is the same, right? It's called mule.env. Ok, we will supply mule.env. Now, it will check the value in the uad.property file. This is the configuration and externalization of the property files that you do every day when you do a project. You can do it through the dollar or through the p function. Creating all the property files here, all of these are done regularly. That's why it's very important to practice it multiple times. If we don't get it in the interview, it will be comfortable again. So just practice it. Okay, it's deployed. I'll try to hit the request. Now, the property file doesn't have any resolve address. Basically, it doesn't have any. Clear and under king. Double quotes. Silence will take it as understood. configuration. configuration. configuration properties global element. There is an extension for this. We have already discussed this. Now, we have a small thing. Now, when you do this particular configuration, if you remember, actually what we are doing is, we have to connect db. db, username, password, all the details are kept in the property file. Can we keep it like that? We can't keep it. Actually, we can't. We have to encrypt that. Basically, what is encrypting? We have seen this before. What is encryption and decryption? For example, we have two systems. This is a system and this is a system. For example, where do we use WhatsApp? This is my WhatsApp. This is my friend's WhatsApp, Ramesh. If I send a message from this WhatsApp, what happens in general? I want to send a message like, Hi Ramesh, how are you? Generally, if you encrypt and send it, it will go like this. Hi Mahesh, how are you? But if you observe, end-to-end conversation is encrypted in WhatsApp. Then what does he do? He encrypts this information. What does encrypting mean? This is a readable format from a readable format. That is, if someone hacks it very easily, it is easily readable. He says Hi Ramesh and Mahesh sent it. It will be easier to understand. He converted from readable format to unreadable format. That means he understood. That is what we call encryption. Conversion of readable format to unreadable format is nothing but encryption. Basically, this process is called encryption. If it is encrypted, it is converted into random strings like x, y, z, 1, 2, 3, 4. Now he will send this encrypted format here. unreadable format. The process of converting unreadable format to readable format. Now what is the secure way? Basically, we convert this and send it. That's why if you observe it in WhatsApp, it will be encrypted. They will convert it and send it. After coming here, this algorithm key will be with them, so they can convert it and read it. Actually, all this will happen automatically. But, this is called encryption and decryption. And again, encryption and decryption are two types. Basically, symmetric encryption and asymmetric encryption. Symmetric means, when you encrypt or decrypt a key, it is called symmetric encryption. When you encrypt a different key and decrypt a different key, it is called asymmetric. We don't have to be experts on this. Simply, if you convert from readable format to unreadable format, it is called encryption. We are using an algorithm key for that. Actually, if you use the same algorithm key while decrypting, it is called a symmetric process. If you use a different key, it is called asymmetric. If you use a different key while decrypting, it is called asymmetric. Asymmetric is more secure. In most of the conversations, when you use a key while decrypting, you use a different key. Then it will be more secure. Any question in this? I will tell you why I am saying this. This. We gave the user name, host, port and password to the database. Is it correct to put the password here? Actually, it is not correct. We should not put it here. Instead, we should encrypt it. We should encrypt such secured information in real time. How should we encrypt? What should we do? How can I put it? When I encrypt, I will put it here. When I consume the property, the encrypted value will not work correctly. Then how will it decrypt? This is the process for us. Did you understand what I said? For example, I encrypted and put a value here. For example, we can use .ml or .properties. After I put it, this app ID value has taken. encrypted value. encrypted value. What do we call it? Secure properties. We secure properties. It's not that simple. So, there is an extra step for this. Add Secure Properties Module from Exchange to Studio. In the latest version, I am using version 7.12. I think you are using version 7.17 or whatever version you are trying to use. In that version, this will be directly in the palette. It will be here. Where? It will be in the Mule Palette, Secure Properties Add Modules. Actually. But... In the previous versions, we had to go to the exchange and import it. Since I am using the old version, how can I tell you that I am using the old version? Go to the help and check about any point studio. What is the version I am trying to use? 7.12.0, basically. Now I am using 7.12.0. Now see if there is a secure in the add module. There is a secure property file. Yes, it is added here too. I imported it from the exchange and it came here too. What should we do with this? We should add this to our project. This is the first step. Now see, this has been added. We will tell you the advantage of adding this. I have done the first step. After this step, I will go to global elements, click create and search for secure. Now see, secure properties have been configured or not. Now, if I don't add this, I am removing this. Now, it is not there. Now, I am going to global elements and see if this secure property configuration will come or not. Did it come? No, it didn't come. No, it didn't come. That means if we add it, it won't come here. That's why we are using it. Do you understand? The reason we need to add this is because that property should be added to the global elements in the project. Okay, I am adding this. Okay, now I will go back. And I will create a secure properties config. This is ok. Look here. Which file should I pick? That's what we are asking. Ok, we will give that file. What is the key? What is the algorithm? What is the mode? We have to decide all these and tell. If we tell, then we can take that particular file and plan everything. The process that I told you now is done in the background. De-grip is done in the background. What is the file now? Which file should we pick? Production file. Production. But it is not production all the time, right? Right. Again the same. Should we do the configuration properties as we did before? Yes. This is it, right? I will take this. Okay. What is the key? I have decided the key. I haven't decided it yet. Basically also. Okay. After deciding that, we will follow these steps. For now, we will ignore this particular step. We will do the next steps. Next step is to encrypt the sensitive data in the property file. What should we do? What is the sensitive data in the property file? There is only the app ID. We should do the same. We have already done it. We have already copied and pasted it. Have you seen this? I will explain you how to do this. There is a website called Secure Properties. If you click on it, you will see this website. You can go here and decrypt and decrypt. This is a website provided by MuleSoft. You can decrypt and decrypt here. For example, I have already decrypted the value. I'm. Okay. If we use prod.properties, it will not work. We haven't used it yet. We used UAT. That's why it worked. For example, this is the prod value. We need this in prod.properties. This is the correct value. Now I have to encrypt this to this particular configuration. That's it, right? I have to encrypt this first, so I am copying this. I went to this website here. I went to this website first. What is this? What should I do with the operation? I have to encrypt. Which algorithm are we using? There are multiple algorithms here. AES and Blowfish use these two algorithms a lot. What is an algorithm? There must be a method to encrypt it. There must be a process. Whatever it is exactly, we are not bothered about what algorithm is about exactly. But AES is fixed. And mode is stated here. This is also used in Maxon default exactly. And what is the key now? Now which key should we encrypt with? I have put the algorithm here. Which key should we encrypt with? For example, let us randomly give x, y, z as 1, 2, 3, 4. We can give it like this. Now if we want to encrypt an A value, we click on generate and click on it. What does it say here? It says that AES key length is not sufficient. When we use this AES algorithm, we need to use 16 bytes. This is 3 and 7 bytes. Now 7 bytes means it has understood. That's why if you observe now. X, Y, Z, 5, 6, 7, 8. Now we get 16 characters. Now we take this key and this algorithm will utilize it and encrypt it. Now when I decrypt, I should have the same key, right? I should have this algorithm. If I know, then I can decrypt. Is it clear? Yes, okay. Look. Now imagine that I have reduced this one letter. It works. That is, in Blowfish, How many letters you give, the key will be sufficient. AIS says that 16 characters should be compulsory, so we are giving this key. What is this key? This is the very important one. Don't share it with anyone. Should I take this value and mention it in the property file? If I mention it directly, will it be sufficient or should I do it differently? You should do it differently. How should I do it? Since we need to know that this is an encrypted value for this particular application, for that, exclamatory mark, square braces, open, square braces, close. Now what this application will understand is that this is an encrypted value. Now all this is not encrypted, this is encrypted, since this is encrypted, we are telling it to decrypt it clearly. What is the syntax for that? exclamatory mark, Square braces. configuration properties. We have to pick this one. But we have to make it dynamic again, right? I have copied and pasted it from there. I am doing it here so that it does not go back again. Here is the dollar. What should we do here? Mule dot. Mule dot en. That's it, right? If not, it will not go randomly to the UAD key, right? Mule dot en. Where is the key? You have to give this. Why are you giving everything locally here? Okay. Okay. Okay. It will take time. What will it do now? It will use the configuration properties file to take all the properties. It will take all the properties from the prod. This is wrong. What will the secure configuration properties file do now? Go there and check the secure configuration properties by checking the exclamation mark. This is the key for this. This is the algorithm. This is the mode. In this property file, we are going to decrypt it in the prod property file and use it to help in this particular configuration. So all this is happening in the background. Is it clear? Yes. Okay fine. Now we shouldn't do hard core here. We should also pass this from the environment. What will I do then? This is also here. $ Curly braces. I gave a name called secure hotkey. That means let's pass it like that. Okay. Okay. multiple secure properties. In real time, there will be a need for that. There will be a separate procedure for that and then there will be another problem. That is to explain to you. That is why I am not going to discuss that exactly. But most of the time, it will be done in 3-4 properties. And this is the way. But there is a way to bulk it. There will be an option to put it in a file and open it. But that is not required at this point of time for you people. Mostly, there will be less in projects. I can cover other content for another half an hour by saying that, right? That's why I'm not doing it. Now, there is only one secure file, the properties file. You can enter these five keys in it. Yes, that's it. For example, you are trying to encrypt all of this. For example, AV, product properties, all of them. Secure all of them and keep the exclamation mark like this. If you keep it, all this will be processed automatically. No problem. Okay. What is the name of the secure file? It is a config file. It is a global file. What does it mean to add multiple files? It means to add multiple files. Okay. Okay. If you give it to prod, it will do it. As you said, even if it is a different security, where are we passing it? We are passing it in that environment. So, it is different for uad and for prod. So, when it is uad, we provide security related to uad. We provide product related to prod. At that time, we are doing it automatically and dynamically. That's why we have this separate voucher. Will it work or not? If we check, will it work for us? Find it server. Business. properties. We need to know the value, so we will debug it. Ok, why is it going wrong? We made a small mistake. Actually, we got it. Actually, we got it because we encrypted it, we gave it, and then we forgot this step. We made a key and gave it as $key. Actually, did you get it? What will happen? It will directly take the encrypted value from the configuration property file. secure colon colon. For example, listener configuration. I forgot that step, that's why we have that problem. Another small thing is, I am running and debugging, right? Did you see here? Debug has a shortcut, run has a shortcut. What this means is, even if we don't run any configuration here, we can use this. If we open this, it will be debugged. Did you see the first one? It will automatically pick the first one. If not, we can open this and use any configuration we want. Run and run configurations are also available. Play button is run. The bug option is for Debug. Even if I click on it, I am doing it by right-clicking, right? If I click on it here, which configuration is in the first place, that configuration will be utilized and the application will be deployed. This is in debug mode. This is the run mode. Exactly. I will use these two options one by one. Then I will tell you again. Okay, we are getting it, right? Yeah, this is overall. Fine, any questions? No, no. Okay. Then we will connect tomorrow. Thanks everyone.
